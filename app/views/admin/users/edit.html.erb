<div class="card">
  <div class="card-header">
    <%= t('.title') %>
  </div>
  <div class="card-body">
    <%= form_for [:admin, @user], html: { class: "form-horizontal" }, multipart: true do |f| %>
      <%= render 'form', f: f %>
    <% end %>
  </div>
</div>
